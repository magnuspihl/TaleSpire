<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- The source predates the folder rename and still declares namespace TaleSpireMapGen. -->
    <RootNamespace>TaleSpireMapGen</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <EmbeddedResource Include="tileset_profiles.json">
      <LogicalName>TaleSpireMapGen.tileset_profiles.json</LogicalName>
    </EmbeddedResource>
  </ItemGroup>

</Project>
